{
  "name": "monika-creation-website",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm install --prefix backend",
    "build": "node scripts/build.js"
  },
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0",
    "mongodb": "^7.3.0"
  }
}
